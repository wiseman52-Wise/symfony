{% extends 'base.html.twig' %}

{% block title %}Edit Etudiants{% endblock %}

{% block body %}
    <h1>Edit Etudiants</h1>

    {{ include('students/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_students_index') }}">back to list</a>

    {{ include('students/_delete_form.html.twig') }}
{% endblock %}
